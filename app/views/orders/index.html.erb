<h1>Orders#index</h1>
<p>Find me in app/views/orders/index.html.erb</p>
<% @orders.each do |order| %>
					<tr>
						<th>
							<!-- itemテーブルのgenre_idカラムに　genre.idを追加し、genre.nameを表示させる -->
<%= order.member.postal_code %>
<%= order.member.address %>
<%= order.member.last_name %>
<%= order.member.first_name %>

						</th>
					</tr>
				<% end %>

